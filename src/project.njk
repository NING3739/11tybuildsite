---
title: Projects
layout: 'base.njk'
description: My personal projects center on frontend web development, occasionally extending to backend work with tools like Flask. 
pagination:
  data: collections.project
  size: 3
  alias: projects
  reverse: true
---

<div class="blog-title">
  <h1>
    <img src="/assets/favicon/project.svg" alt="favicon">
      &nbsp;{{ title }}</h1>
    <p>{{ description }}</p>
  </div>
  {% include 'project-list.njk' %}
  <!-- Pagination Links -->
  <nav class="pagination">
    {%- if pagination.href.previous %}
      <a href="{{ pagination.href.previous }}" class="pagination-previous">Previous</a>
    {%- else %}
      <a>Previous</a>
    {%- endif %}
    {%- if pagination.href.next %}
      <a href="{{ pagination.href.next }}" class="pagination-next">Next</a>
    {%- else %}
      <a>Next</a>
    {%- endif %}
  </nav>
  <hr>
    <!-- categories list -->
    <div class="categories">
      <h3>Categories:</h3>
      <ul>
        {% for category in collections.projectcategories %}
          <li>
            <a href="/project/{{ category.slug }}">{{ category.title | capitalize }}</a>
          </li>
        {% endfor %}
      </ul>
    </div>